<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Naveasy.Samples.Views.PageC"
             x:DataType="viewModels:PageCViewModel"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Naveasy.Samples.ViewModels">
    <ScrollView>
        <VerticalStackLayout Spacing="25"
                             Padding="30,0">
            <Label Text="Page C Content"
                   Margin="15"
                   Style="{StaticResource Headline}" />

            <Button Command="{Binding NavigateCommand}" Text="Go D"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
