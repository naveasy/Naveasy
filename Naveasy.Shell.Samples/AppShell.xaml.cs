namespace Naveasy.Shell.Samples;

// MauiShell is an alias of Microsoft.Maui.Controls.Shell: this sample lives inside the Naveasy.Shell
// namespace, which hides the type name. An app with its own namespace just writes ": Shell".
public partial class AppShell : MauiShell
{
    public AppShell()
    {
        InitializeComponent();
    }
}
